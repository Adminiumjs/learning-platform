/*
 * Q&A inbox — page-local seed.
 */

/**
 * What "Draft for me" drops into the reply box.
 *
 * A canned house answer, not a model call: the demo makes no network requests,
 * and the button is honest about being a template you then edit.
 */
export const SUGGESTED_ANSWER =
  "Good question. Here's how I'd think about it: name the rule you land on, write it down where the next person will find it, and we'll pressure-test it on Thursday.";
