/* PORT-STUB: inbox — replace with the ported screen. */

import { EmptyState } from "../components";
import "../styles/screen-inbox.css";

export default function Inbox() {
  return (
    <div className="lp-page scr-inbox">
      <EmptyState icon="box" title="Q&A inbox" body="Not ported yet." />
    </div>
  );
}
