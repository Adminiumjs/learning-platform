/* PORT-STUB: notes — replace with the ported screen. */

import { EmptyState } from "../components";
import "../styles/screen-notes.css";

export default function Notes() {
  return (
    <div className="lp-page scr-notes">
      <EmptyState icon="box" title="Notes" body="Not ported yet." />
    </div>
  );
}
