/* PORT-STUB: cohort — replace with the ported screen. */

import { EmptyState } from "../components";
import "../styles/screen-cohort.css";

export default function CohortSetup() {
  return (
    <div className="lp-page scr-cohort">
      <EmptyState icon="box" title="Cohort setup" body="Not ported yet." />
    </div>
  );
}
