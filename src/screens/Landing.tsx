/* PORT-STUB: landing — replace with the ported screen. */

import { EmptyState } from "../components";
import "../styles/screen-landing.css";

export default function Landing() {
  return (
    <div className="lp-page scr-landing">
      <EmptyState icon="box" title="Course landing page" body="Not ported yet." />
    </div>
  );
}
